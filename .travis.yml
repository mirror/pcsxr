language: c
compiler:
  - clang
  - gcc
  
addons:
  apt:
    packages:
      - autoconf-archive
      - intltool
      - libgtk-3-dev
      - libsdl2-dev
      - libxtst-dev
      - libxml2-utils

script:
  - ./autogen.sh
  - make
